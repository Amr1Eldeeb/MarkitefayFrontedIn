import { ScorenumberPipe } from './scorenumber-pipe';

describe('ScorenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ScorenumberPipe();
    expect(pipe).toBeTruthy();
  });
});
